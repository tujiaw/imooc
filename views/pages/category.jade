//
   Created by tujiaw on 15/8/29.

extends ../frame
block content
   h3.text-center 文章类别管理页面
   .container
      .row
         table.table.table-hover.table-hordered
            thead
               tr
                  th 类别
                  th 数目
                  th 查看
                  th 更新
                  th 删除
            tbody
               each item in categories
                  tr(class="item-id-#{item._id}")
                     td #{item.name}
                     td #{item.count}
                     td: a(target="_blank", href="#{item._id}") 查看
                     td
                        button.btn.btn-warning.update(type="button", item-id="#{item._id}") 更新
                     td
                        button.btn.btn-danger.del(type="button", item-id="#{item._id}") 删除
      .row
         .col-md-4.input-group
            input.form-control(type="text", placeholder="请输入新的类别名称")
            span.input-group-btn
               button.btn.btn-primary#addCategory(type="button") 添加
script(type="text/javascript" src="/js/category.js")
